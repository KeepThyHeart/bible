/**
 * Topical-index commands. Scaffold; populated as the TopicsPane is migrated.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerTopicsCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
