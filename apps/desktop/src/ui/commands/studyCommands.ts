/**
 * Study-resources commands. Scaffold; populated as study features migrate.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerStudyCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
