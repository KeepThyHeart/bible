/**
 * Dictionary-pane commands. Scaffold; populated as the DictionaryPane is migrated.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerDictionaryCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
