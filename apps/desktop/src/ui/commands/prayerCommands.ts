/**
 * Prayer-list commands. Scaffold; populated as the prayer panes are migrated.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerPrayerCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
