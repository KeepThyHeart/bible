/**
 * Book-pane commands. Scaffold; populated as the BookPane is migrated.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerBookCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
