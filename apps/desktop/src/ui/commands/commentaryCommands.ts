/**
 * Commentary-pane commands. Scaffold; populated as the CommentaryPane is
 * migrated through the registry.
 */

import type { ICommandRegistry } from '../services/ICommandRegistry';
import type { IDisposable } from '../types/Command';

export function registerCommentaryCommands(_registry: ICommandRegistry): IDisposable[] {
  void _registry;
  return [];
}
