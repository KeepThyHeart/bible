/**
 * The app-wide content-source seam (task 0034, finishing 0029's S3a) - DTO
 * types plus the ten Promise-returning provider interfaces built over them.
 * See `interfaces.ts`'s own doc comment for why these, not the module
 * repositories, are what a remote/licensed content source implements.
 */
export * from './dto';
export * from './interfaces';
