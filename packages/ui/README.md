# @bible/ui

Shared UI for the desktop and web apps and (later) extension panels: a small component set and the KTH CSS
framework. Private, GPL-3.0-or-later for now (the licence of the kit sources and `css/` is still to be decided).

## Package rules

- **React 18 API subset only.** Components run on real React (desktop) and on `preact/compat` (web, which aliases
  `react`). Do not use React 19 APIs (`use`, `useTransition`, `useDeferredValue`, `useOptimistic`, `flushSync`, ...).
  `eslint.config.mjs` enforces this and it runs as `pretest`, so `npm test` (and CI) fails on a violation.
- **Consumed as source.** The apps alias `@bible/ui` to `packages/ui/src` (tsconfig `paths` plus the Vite alias) and
  `@bible/ui/css/*` to `packages/ui/css`. There is no build step. Never import `preact` here.
- **No app coupling.** No imports from apps, app stores, `zustand`, `react-i18next`/`i18next` (take labels as props with
  English defaults), `electron`, or the `@bible/core` barrel (use `@bible/core/browser`).
- **Sandbox-safe.** No `localStorage`/`sessionStorage`/`fetch`, no `dangerouslySetInnerHTML`/`innerHTML`, no `eval`.
- **Styling is `kth-*` classes over `--kth-*` tokens only.** The linter sees only static class strings; dynamic class
  building (template literals, `clsx`) with Tailwind or app classes cannot be caught, so do not do it.
- **Tests run twice**, once on React and once with `react` aliased to `preact/compat` (`vitest.config.ts` projects
  `react` and `preact`). Import from `@testing-library/react` (swapped to `@testing-library/preact` in the preact
  project) and `@testing-library/user-event` only; no RTL-only options and no `React.act`.

## KTH CSS (`css/`)

| File | Role |
|---|---|
| `kth.css` | Entry: contract + dark defaults + classes. Apps import `generated/map-<app>.css` first, then this. |
| `kth-contract.css` | Declares every `--kth-*` token (fallback values), inside `:where(:root)`. |
| `kth-classes.css` | Opt-in `.kth-*` classes (single-class specificity, logical properties, tokens only). |
| `kth-base.css` | Reset and typography for extension iframes only. The apps must not import it. |
| `generated/` | GENERATED by `scripts/generate-kth-css.js`: per-app token maps and dark defaults. |

Adding a token: declare it in `kth-contract.css`, add it to `KTH_MAP` (or `CONTRACT_CONSTANTS`) in
`scripts/generate-kth-css.js`, run `npm run generate:kth-css`. `npm run check:kth-css` (part of `test:scripts`) fails
when the generated files are stale; `css/kth-css.test.ts` checks the contract, the maps and the class rules.

`--kth-*` resolve on `<html>` and descendants inherit the computed value, so a subtree that re-declares `--theme-*`
or `--bg-*` locally (for example a theme preview card) does not re-resolve them.
